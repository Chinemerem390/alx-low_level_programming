#include <stdio.h>
/**
 * main - Print all basd 16 num
 *
 * Description: Must be in Lowercase
 * Return: return (0)
 */
int main(void)
{
char num;
for (num = '0'; num < '10'; num++)
{
	putchar(num);
}
for (num = 'a'; num < 'g'; num++)
{
	putchar(num);
}
putchar('\n');
return (0)i;
}
